# M4A 受控上传、OCR 契约与逐项校对验证记录

> 日期：2026-09-11  
> 里程碑：M4A  
> 数据：仅合成输入

## 1. 已完成范围

- PDF、PNG、JPEG 的扩展名、MIME、文件签名、空文件与 5 MB 上限校验；
- 合成文件写入 Git 忽略的本地私有目录，使用随机对象键，源文件不通过静态目录公开；
- 只有活动授权的所属参与者可以通过 API 下载源文件，响应禁止共享缓存；
- OCR 适配器契约、8 秒超时、最多 2 次尝试、失败状态、稳定错误码和人工重试；
- 指标原文、首次提取值、置信度、页码和归一化位置持久化；
- 逐字段确认或修正，以及全部字段处理后的整份报告确认；
- 排序服务同时校验报告与所有指标的确认状态；
- Alembic 迁移 `a4b5c6d7e8f9_report_ocr_review` 与文档同步。

## 2. 自动化结果

| 检查 | 结果 |
|---|---|
| Python 编译检查 | 通过 |
| Pytest | 45 项通过；2 条来自第三方测试客户端的弃用警告 |
| Alembic 现有数据库升级 | 通过 |
| Alembic 模型差异检查 | `No new upgrade operations detected` |
| Vue/TypeScript 类型检查 | 通过 |
| 前端生产构建 | 通过 |
| 全新数据库升级、降级、再升级 | 通过 |

核心回归断言包括：文件签名不匹配被拒绝；路径型文件名被清理；OCR 失败报告的指标数组为空；未逐项确认时整份确认返回 409；未确认数据无法进入排序；修正后原始提取值仍保留；私有下载响应携带 `Cache-Control: private, no-store`。

## 3. 未执行与外部依赖

- 未接入或调用真实华为云 OBS；本地私有目录不是 OBS 成功证据。
- 未接入或调用真实华为云 OCR；合成场景不是准确率评测。
- 未使用真实报告或真实个人健康数据。
- 按现有站点验证规范，本轮未在未获明确要求的情况下追加浏览器自动化走查；前端以类型检查和生产构建作为代码门禁。

真实 OBS、真实 OCR、脱敏样例评测和云端访问证据仍是第 4 周后续任务，不以本记录替代。
